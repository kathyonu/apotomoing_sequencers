module SequenceCreatedsHelper
  include ApplicationHelper

# include Histogram
# include Lexigram
# include Textual

  # TODO what is the purpose of this code ? is it used ? is it tested ? what does it affect ? 
#  def sequence_lexigram(sequencetext)
#    @sequence_lexigram = lexigram_sequencer(sequencetext)
#  end
  
end
